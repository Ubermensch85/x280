###########################
### 🖥️ RENDERING & GPU ###
###########################
vo=gpu                       # Più stabile e leggero di gpu-next su Intel Gen 9.5
gpu-context=wayland          # Backend nativo Wayland
hwdec=auto-safe              # La scelta migliore per VAAPI su Arch

###########################
### 🎞️ QUALITÀ & SCALING #
###########################
# Rimosso profile=gpu-hq (troppo pesante per X280)
scale=bilinear               # Velocissimo, zero carico CPU
cscale=bilinear
dscale=bilinear
# interpolation=no           # Disattivato per risparmiare batteria

###########################
### 🔊 AUDIO & SYNC ###
###########################
audio-display=no
ao=pipewire
volume-max=150
video-sync=display-resample  # Mantiene il video fluido senza tearing

###########################
### 🎛️ UTILITY ###
###########################
keep-open=yes
save-position-on-quit=yes
# Formato ottimizzato per evitare codec VP9 pesanti in 4K se non necessari
ytdl-format="bestvideo[height<=1080][vcodec!=vp9]+bestaudio/best"
